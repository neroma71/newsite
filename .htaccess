RewriteEngine On

# Serve les fichiers existants directement
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon tout passer à l'index.php de la racine
RewriteRule ^ index.php [L]